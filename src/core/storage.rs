//! TODO

use std::path::Path;

use crate::model::Vault;
use crate::error::Result;

pub fn write_vault_file(path: &Path, vault: &Vault) -> Result<()> {
    !todo!()
}

pub fn read_vault_file(path: &Path) -> Result<Vault> {
    !todo!()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_() {}
}
